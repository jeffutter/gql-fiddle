# Rust
/target
**/target

# WASM build output
crates/gql-core/pkg/
web/src/wasm/

# Node / web
node_modules/
web/dist/*
!web/dist/.gitkeep
web/node_modules/
# live-sync is an npm project — ignore pnpm artifacts if pnpm is invoked there by mistake
live-sync/pnpm-lock.yaml
live-sync/pnpm-workspace.yaml

# direnv / nix
.direnv/

# editor / OS
.DS_Store
*.swp

# Cloudflare / wrangler local state
.wrangler/

# Local wrangler secrets (copy .dev.vars.example → .dev.vars and fill in values)
.dev.vars

# Generated by `wrangler types` — re-run after editing wrangler.jsonc
worker-configuration.d.ts

# local Claude settings
.claude/settings.local.json

# overnight backlog loop logs
loop-logs/

tests/__pycache__/
